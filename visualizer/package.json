{
  "name": "aoe2-replay-visualizer",
  "version": "1.0.0",
  "description": "Age of Empires II Replay Visualizer",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler pages dev . --port 8000",
    "deploy": "wrangler pages deploy .",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "recage": "^0.1.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
